{
  "name": "mindflow",
  "productName": "MindFlow",
  "version": "1.2.3",
  "description": "本地优先的笔记、工作流与思维导图桌面应用",
  "private": true,
  "type": "module",
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "electron": "electron .",
    "electron:dev": "concurrently -k \"vite --host 127.0.0.1 --port 5173 --strictPort\" \"wait-on http://127.0.0.1:5173 && electron .\"",
    "package:win": "set \"ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/\" && pnpm run build && electron-packager . MindFlow --platform=win32 --arch=x64 --out=release --overwrite --asar --icon=assets/mindflow-app-icon.ico --ignore=node_modules --ignore=src --ignore=release --ignore=scripts --ignore=artifacts --ignore=design-reference.png --ignore=design-qa.md --ignore=pnpm-lock.yaml --ignore=pnpm-workspace.yaml --ignore=AGENTS.md --app-version=1.2.3 --win32metadata.CompanyName=MindFlow --win32metadata.FileDescription=MindFlow --win32metadata.ProductName=MindFlow"
  },
  "dependencies": {
    "@phosphor-icons/react": "^2.1.10",
    "@vitejs/plugin-react": "5.0.4",
    "@xyflow/react": "^12.11.2",
    "react": "19.2.0",
    "react-dom": "19.2.0",
    "vite": "6.4.2"
  },
  "devDependencies": {
    "@electron/packager": "^20.0.2",
    "concurrently": "^10.0.3",
    "electron": "^43.1.0",
    "wait-on": "^9.0.10"
  }
}
